{
  "name": "aprova-mais",
  "version": "1.0.0",
  "private": true,
  "description": "Plataforma pré-vestibular para ENEM e UERJ — planner, foco, simulados e leitura de progresso.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-deploy.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
